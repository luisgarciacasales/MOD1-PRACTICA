"""Etapa 5 — Transformación Gold de datos de mercado.

Calcula retornos diarios, medias móviles 7d/30d y volatilidad 30d sobre
silver_market_prices; normaliza las series macro con variación interanual.

STUB DEL SCAFFOLD — implementación guiada por el skill `medallion-pipeline`.
"""

import sys


def main() -> int:
    print(
        "[transform] etapa no implementada — este es el andamiaje.\n"
        "  Guía: .claude/skills/medallion-pipeline/SKILL.md",
        file=sys.stderr,
    )
    return 1


if __name__ == "__main__":
    raise SystemExit(main())
