"""Etapa 7 — Indexación vectorial (Gold → FAISS).

Vectoriza los registros enriquecidos y construye/actualiza el índice FlatIP.
Expone search_semantic(query, top_k) y get_market_context(ticker).

STUB DEL SCAFFOLD — implementación guiada por el skill `medallion-pipeline`.
"""

import sys


def main() -> int:
    print(
        "[index] etapa no implementada — este es el andamiaje.\n"
        "  Guía: .claude/skills/medallion-pipeline/SKILL.md",
        file=sys.stderr,
    )
    return 1


if __name__ == "__main__":
    raise SystemExit(main())
